user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 768;
}

http {
    include /etc/nginx/conf.d/*.conf;

    server {
        listen 80;
        location / {
            proxy_pass http://checkout_svc;
        }
    }
}
